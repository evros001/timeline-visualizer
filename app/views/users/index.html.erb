<h1>All the Users:</h1>

<ul>
  <% @users.each do |user| %>
    <li><%= user.name.capitalize %> | 
      <%= link_to "ADD Story", new_user_story_path(user.id) %> |
      <%= link_to "VIEW User Page", user_path(user.id) %><br><br></li>
  <% end %>
</ul>
